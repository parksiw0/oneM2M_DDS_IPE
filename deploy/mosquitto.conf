# Local demo broker. Compose publishes both listeners on loopback by default.
allow_anonymous true
persistence false
log_dest stdout
connection_messages true

# TinyIoT publishes notifications via the MQTT TCP binding.
listener 1883
protocol mqtt

# The browser receives the same MQTT messages over WebSocket.
listener 9001
protocol websockets
